import Vue from 'vue'
import axios from 'axios'

// initial state
const state = {
	serverUrl: ''
}

// getters
const getters = {
}

// actions
const actions = {
	
}

// mutations
const mutations = {
	
}

export default {
  state,
  getters,
  actions,
  mutations
}